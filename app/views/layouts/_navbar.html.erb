<nav class="navbar navbar-dark navbar-expand-lg bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><%= t('koombea') %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 right">
        <li class="nav-item">
          <%= link_to t('helpers.links.products'), products_path, class: 'btn btn-link' %>
        </li>
        <li class="nav-item">
          <%= link_to t('cart_items.index.title'), cart_items_path, class: 'btn btn-warning' %>
        </li>
        <li class="nav-item">
          <%= button_to session_path(current_user), method: :delete, class: 'btn btn-warning' do %>
              <%= t('helpers.submit.exit') %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
